<!-- pages/list/list.wxml -->
<view class="container">
    <view class="item" wx:for="{{movies}}" wx:key="unique" wx:for-item="movie">
        <movie-item class="items" movie="{{movie}}" />
    </view>
</view>